# ============================================================
# 核心依赖（必装）
# ============================================================
requests>=2.28
gradio>=4.0
jieba>=0.42
pdfplumber>=0.10
python-docx>=1.0
tenacity>=8.0

# ============================================================
# 可选：统一 LLM 接口（MIT, https://github.com/BerriAI/litellm）
# 设置 LLM_MODEL 环境变量后自动启用（支持100+模型商）
# 安装: pip install litellm
# ============================================================
# litellm>=1.40

# ============================================================
# 可选：FSRS 间隔重复调度（MIT, https://github.com/open-spaced-repetition/py-fsrs）
# plan 命令加 --fsrs 启用
# 安装: pip install fsrs
# ============================================================
# fsrs>=4.0

# ============================================================
# 可选：pkuseg 医学领域分词（MIT, https://github.com/lancopku/pkuseg-python）
# 设置 USE_PKUSEG=1 启用
# 安装: pip install pkuseg
# ============================================================
# pkuseg>=0.0.22

# ============================================================
# 可选：PaddleOCR 扫描版真题/图片识别（Apache 2.0）
# 设置 USE_OCR=1 启用；扫描版PDF还需 pymupdf 渲染页面
# 安装: pip install paddleocr pymupdf
# ============================================================
# paddleocr>=2.7
# pymupdf>=1.24

# ============================================================
# 可选：错题导出 Anki 卡包（MIT, https://github.com/kerrickstaley/genanki）
# wrong export --format anki
# 安装: pip install genanki
# ============================================================
# genanki>=0.13

# ============================================================
# 可选：错题导出 Excel（MIT, https://github.com/openpyxl/openpyxl）
# wrong export --format excel
# 安装: pip install openpyxl
# ============================================================
# openpyxl>=3.1

# ============================================================
# 可选：相似题检索（Apache 2.0, https://github.com/dorianbrown/rank_bm25）
# similar 命令：本地离线 BM25 检索相似真题
# 安装: pip install rank_bm25
# ============================================================
# rank_bm25>=0.2.2
